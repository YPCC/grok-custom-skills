#!/usr/bin/env python3
"""Look up a DOI or arXiv ID for a single paper title.

This is a thin wrapper entry point. After cloning, run:

    bash scripts/fetch_upstream_scripts.sh

to replace this stub with the full implementation from
isaaccorley/skills (stdlib-only, Crossref + arXiv + Semantic Scholar).
"""
import sys
print(
    "lookup_id.py: full script not yet fetched.\n"
    "Run:  bash scripts/fetch_upstream_scripts.sh\n"
    "from the skills/bib-audit/ directory.",
    file=sys.stderr,
)
sys.exit(2)
